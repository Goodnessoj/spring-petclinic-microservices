# Customers Service

Manages owner and pet data for the Spring Petclinic microservices application.

This service is built and deployed automatically via the `build-push.yml` GitHub Actions workflow, which pushes the resulting ARM64 image to Amazon ECR.

<!-- Build trigger: 2026-06-28 customers-service image rebuild after Trivy fix. -->
